import styled from 'styled-components';

export const Image = styled.img`
  position: relative;
  width: 100%;
  z-index: -1;
`;

export const Paragraph = styled.p`
  position: absolute;
  top: 0;
  color: white;
  padding: 1em;
  font-size: 24px;
`;
